# Disabled inherited workflows

The inherited Dabdoob macOS signing workflow is parked here until Lacapult Doobdab has its own packaging/release process. It still references Dabdoob asset names and should not run in the public repo by accident.
